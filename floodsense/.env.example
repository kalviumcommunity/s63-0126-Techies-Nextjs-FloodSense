# ===================================================
# ENVIRONMENT VARIABLES TEMPLATE — FloodSense
# Copy this file to `.env.local` and replace values
# ===================================================

# DATABASE (Server Only)
DATABASE_URL="postgresql://username:password@localhost:5432/floodsense"

# REDIS CACHE (Server Only)
REDIS_URL="redis://localhost:6379"

# AUTH SECRET (Server Only)
AUTH_SECRET="your_auth_secret_here"

# JWT SECRET (Server Only)
JWT_SECRET="your_jwt_secret_here"

# WEATHER API KEY (Server Only)
OPENWEATHER_API_KEY="your_openweather_key_here"

# PUBLIC VARIABLES (Safe for Client)
NEXT_PUBLIC_APP_NAME="FloodSense"
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api"
